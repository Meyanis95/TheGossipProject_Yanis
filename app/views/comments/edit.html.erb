<h1>Editer le comentaire</h1>

<%= form_for @comment do |f|%>

  <div class = "form-group">
    <label>Contenu du gossip</label>
    <%= f.text_area :content, class: 'form-control'%>
  </div>
  <div class = "form-group">
    <button type="submit">Modifier le commentaire</button>
  </div>

<% end%>