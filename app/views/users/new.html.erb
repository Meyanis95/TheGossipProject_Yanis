<html>

  <body>
    <div class="jumbotron jumbotron-fluid">
      <div class="container">
        <h1 class="display-4"> Ici tu peux t'inscrire !</h1>
        <p> C'est parti !</p>
      </div>
    </div>
   
   <div class="container mt-5">
    <%= form_tag url_for(action: 'create'), method: "POST" do %>

    <div class="form-group">
      <%= label_tag 'Prénom' %>
      <%= text_field_tag 'first_name', params['first_name'], class: 'form-control'%>
    </div>

    <div class="form-group">
      <%= label_tag 'Nom' %>
      <%= text_field_tag 'last_name', params['last_name'], class: 'form-control'%>
    </div>

    <div class="form-group">
      <%= label_tag 'Age' %>
      <%= text_field_tag 'age', params['age'], class: 'form-control'%>
    </div>

    <div class="form-group">
      <%= label_tag 'Description' %>
      <%= text_field_tag 'description', params['description'], class: 'form-control'%>
    </div>

    <div class="form-group">
      <%= label_tag 'Email' %>
      <%= text_field_tag 'email', params['email'], class: 'form-control'%>
    </div>

    <div class="form-group">
      <%= label_tag 'Mot de passe' %>
      <%= text_field_tag 'password', params['password'], class: 'form-control'%>
    </div>

    <div class="form-group">
      <%= label_tag 'confirmer votre mot de passe' %>
      <%= text_field_tag 'password_confirmation', params['password_confirmation'], class: 'form-control'%>
    </div>

      <%= submit_tag "Valide ton inscription !", class: "btn btn-primary" %>

    <% end %>
   </div>

  </body>
</html>